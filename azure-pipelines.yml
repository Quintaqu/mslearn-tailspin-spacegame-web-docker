trigger:
- '*'

variables:
  webRepository: 'web'
  tag: '$(Build.BuildId)'

stages:
- stage: 'Build'
  displayName: 'Build and push Docker image'
  jobs: 
  - job: 'Build'
    displayName: 'Build Docker container'
    pool:
      vmImage: 'ubuntu-20.04'
    steps:
    - task: Docker@2
      displayName: 'Build and push image to ACR'
      inputs:
        command: buildAndPush
        containerRegistry: 'Container Registry Connection'  # Must match your service connection name
        repository: '$(webRepository)'
        dockerfile: '$(Build.SourcesDirectory)/Tailspin.SpaceGame.Web/Dockerfile'
        buildContext: '$(Build.SourcesDirectory)/Tailspin.SpaceGame.Web'
        tags: |
          $(tag)

- stage: 'Deploy'
  displayName: 'Deploy the container to App Service'
  dependsOn: Build
  jobs:
  - job: 'Deploy'
    displayName: 'Deploy job'
    pool:
      vmImage: 'ubuntu-20.04'
    variables:
    - group: Release  # Make sure WebAppName and RegistryName exist in this group
    steps:
    - task: AzureWebAppContainer@1
      displayName: 'Deploy container to App Service'
      inputs:
        azureSubscription: 'Resource Manager - Tailspin - Space Game'  # Must match Azure service connection
        appName: '$(WebAppName)'
        containers: '$(RegistryName)/$(webRepository):$(tag)'
